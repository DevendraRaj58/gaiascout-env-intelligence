# ============================================================
# GaiaScout Backend — Python Dependencies
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9      # for file/form uploads
sse-starlette==2.1.0         # Server-Sent Events for streaming

# --- Environment & Config ---
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.1

# --- LangChain Ecosystem (the agent orchestration layer) ---
langchain
langchain-core
langchain-community
langchain-google-genai   # Gemini integration
langchain-openai==0.1.14        # OpenAI fallback integration
langgraph==0.1.14               # Stateful graph agent (NOT just chains)

# --- Vector Database (Semantic Knowledge Layer) ---
chromadb

# --- Hybrid Search (BM25 keyword search — our differentiator) ---
rank-bm25

# --- Embeddings (runs FULLY LOCAL — no API key needed) ---
sentence-transformers
torch==2.5.1                    # CPU version; avoids GPU requirement
transformers==4.44.2
# --- SQLite (Structured Knowledge Layer) ---
# sqlite3 is part of Python stdlib — no install needed

# --- Geo Context (Bonus Feature) ---
geopy
reverse-geocoder

# --- Data Processing ---
numpy==1.26.4
pandas==2.2.2

# --- HTTP Client (for any external API calls) ---
httpx

# --- Logging & Utilities ---
rich==13.7.1                    # Pretty terminal output
tenacity==8.3.0                 # Retry logic for API rate limits
